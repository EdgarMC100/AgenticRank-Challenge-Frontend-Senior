# Production environment variables (example)
# Copy this to .env.production and update with your actual values
# Or set these as environment variables in AWS Amplify Console

# Backend API URL - will be set to AWS App Runner URL after backend deployment
VITE_API_URL=https://your-backend-url.awsapprunner.com
